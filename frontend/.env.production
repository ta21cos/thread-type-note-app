# Frontend Production Environment Variables
# NOTE: For both Cloudflare Pages (web) and Electron (desktop) deployment

# NOTE: Backend API endpoint
VITE_BACKEND_API_ENDPOINT=https://thread-note-backend-production.e-st1-ta2altr5.workers.dev

# NOTE: WebSocket endpoint removed - using polling instead for Cloudflare compatibility

NODE_ENV=production
